const SortForm = ({setAllArticles}) => {
  return (
    <div className="sortForm">
      <form>
        <fieldset>
          <legend>Sort</legend>
        </fieldset>
      </form>
    </div>
  );
};

export default SortForm;
